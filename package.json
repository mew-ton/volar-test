{
  "name": "volar-test",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "watch": "turbo watch",
    "format": "biome format --write .",
    "lint": "biome check --apply ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.7.3",
    "@types/bun": "latest",
    "language-server": "file:./packages/language-server",
    "turbo": "^1.13.3"
  }
}
